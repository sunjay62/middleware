import PaymentModal from '@/components/PaymentModal/PaymentModal';
import React from 'react';

const page = () => {
  return (
    <div>
      <div>
        <PaymentModal />
      </div>
    </div>
  );
};

export default page;
